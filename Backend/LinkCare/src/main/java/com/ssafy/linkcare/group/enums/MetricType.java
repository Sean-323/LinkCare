package com.ssafy.linkcare.group.enums;

/**
 * 목표 메트릭 타입
 * - STEPS: 걸음수
 * - KCAL: 칼로리
 * - DURATION: 운동시간
 * - DISTANCE: 이동거리
 */
public enum MetricType {
    STEPS,      // 걸음수
    KCAL,       // 칼로리
    DURATION,   // 운동시간 (분)
    DISTANCE    // 이동거리 (km)
}
